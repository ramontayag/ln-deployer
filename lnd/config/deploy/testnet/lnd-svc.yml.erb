---
apiVersion: v1
kind: Service
metadata:
  name: lnd
  <<: <%= partial "namespace" %>
  labels:
    app: lnd
spec:
  ports:
    - port: 9735
      targetPort: server-port
      name: server-port
    - port: 1009
      targetPort: rpc-port
      name: rpc-port
  selector:
    app: lnd
